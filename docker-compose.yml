services:
  mpesa-mock:
    image: ghcr.io/smbugua/mpesa-mock:latest
    ports:
      - "4000:4000"
    environment:
      MPESA_MOCK_DELAY: "2000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4000/__mock__/health"]
      interval: 10s
      timeout: 3s
      retries: 3
